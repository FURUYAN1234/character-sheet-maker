# Character Sheet Maker v1.4.2

## What's New / 更新内容

- Every composed PNG is now exactly `1120x1584`, with a `70:99` aspect ratio matching A4 (`210:297`). / 合成後のPNGを `1120x1584` に固定し、A4（`210:297`）と同じ `70:99` にそろえました。
- The complete provider artwork is fitted below the Japanese profile header without cropping or changing its aspect ratio. / 生成画像は日本語プロフィール欄の下に、トリミングせず縦横比を保って配置します。
- Added a focused regression test for the exact output dimensions, A4 ratio, and uncropped artwork scaling. / 出力寸法、A4比率、生成画像全体の縮小配置を確認する回帰テストを追加しました。
